/**
 * Created by peterparker on 16-04-05.
 */
alert('welcome');
//to do
//add a javascript code that creates paragraphs with the words in the array array1 in the div with id = div1
var array1 = ['Apple','Orange','Banana','Melon'];
var returnFruitNamesWithO = function(sampleArray){
  for(var i=0; i<4; i++){
    if(sampleArray[i] == 'Apple'){
      alert(sampleArray[i]);
    }
  }
}

returnFruitNamesWithO(array1);

//ToDo1:
//Create a function that returns fruit names that starts with 'A' or 'B' only

//ToDo2:
//Create a function that outputs the first two characters of each fruit names

//ToDo3:
//Create two arrays
//Push fruit names that start with A-H into the first array and the rest into the other array
